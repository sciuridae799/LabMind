# Agent Rules

本项目中的任何代码修改，必须遵守以下规则：

1. 必须基于第一性原理理解需求、本质约束、数据流和状态流。
2. 不允许给出兼容性方案、补丁性方案、兜底方案、降级方案。
3. 不允许为需求之外的场景主动设计额外逻辑。
4. 不允许过度设计，不允许引入不必要抽象。
5. 必须优先修改根因位置，禁止在外围包裹特判分支。
6. 如果旧逻辑错误，直接改为正确逻辑，不保留错误行为兼容。
7. 不允许用默认值、fallback、吞错等方式掩盖业务问题。
8. 所有改动必须保证全链路逻辑一致，而不是只让局部代码“能跑”。
9. 所有方案必须包含全链路验证：输入、处理、输出、上下游影响、边界条件、失败路径、回归影响。
10. 输出不套模板，不罗列无效内容。但必须抓住本质，讲清楚核心问题、错误根因、正确改动点、链路影响，以及如何做全链路验证。
11. 涉及 Java/Maven 构建、测试、验证时，必须使用我本地环境中的 Maven，不允许使用默认 `~/.m2`，不允许隐式回落到系统默认仓库。
12. 每次更改后删除不需要的.gitkeep

目标：只做正确且必要地实现，不做模糊、折中、补丁式交付。
