spring:
  datasource:
    url: jdbc:mysql://43.139.223.137:3306/service_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 43.139.223.137
      port: 6379
      password: 123456
      database: 0
  ai:
    dashscope:
      # Shared by the chat and embedding model chain.
      api-key: ${ALI_BAI_LIAN_API_KEY}
    alibaba:
      toolcalling:
        tavilysearch:
          api-key: ${TAVILY_API_KEY}
  redis:
    redisson:
      protocol: redis
      local-lock-cache-ttl: 30s
