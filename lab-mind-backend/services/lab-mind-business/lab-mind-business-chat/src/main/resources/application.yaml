spring:
  datasource:
    url: ${LAB_MIND_MYSQL_URL}
    username: ${LAB_MIND_MYSQL_USERNAME}
    password: ${LAB_MIND_MYSQL_PASSWORD}
    driver-class-name: ${LAB_MIND_MYSQL_DRIVER_CLASS_NAME}
  data:
    redis:
      host: ${LAB_MIND_REDIS_HOST}
      port: ${LAB_MIND_REDIS_PORT}
      password: ${LAB_MIND_REDIS_PASSWORD}
      database: ${LAB_MIND_REDIS_DATABASE}
  kafka:
    bootstrap-servers: ${LAB_MIND_KAFKA_BOOTSTRAP_SERVERS}
    admin:
      auto-create: ${LAB_MIND_KAFKA_ADMIN_AUTO_CREATE:true}
    properties:
      security.protocol: ${LAB_MIND_KAFKA_SECURITY_PROTOCOL:SASL_PLAINTEXT}
      sasl.mechanism: ${LAB_MIND_KAFKA_SASL_MECHANISM:PLAIN}
      sasl.jaas.config: org.apache.kafka.common.security.plain.PlainLoginModule required username="${LAB_MIND_KAFKA_USERNAME}" password="${LAB_MIND_KAFKA_PASSWORD}";
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
    consumer:
      group-id: lab-mind-business-chat
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      auto-offset-reset: earliest
      enable-auto-commit: false
    listener:
      auto-startup: ${LAB_MIND_KAFKA_LISTENER_AUTO_STARTUP:true}
  ai:
    dashscope:
      # Shared by the chat and embedding model chain.
      api-key: ${ALI_BAI_LIAN_API_KEY}
      chat:
        options:
          model: qwen-plus-latest
    alibaba:
      toolcalling:
        tavilysearch:
          api-key: ${TAVILY_API_KEY}
  redis:
    redisson:
      protocol: redis
      local-lock-cache-ttl: 30s
lab-mind:
  paper-graph:
    service:
      base-url: ${LAB_MIND_PAPER_GRAPH_SERVICE_BASE_URL}
      internal-token: ${LAB_MIND_PAPER_GRAPH_INTERNAL_API_TOKEN}
  chat:
    model-api-config:
      api-key-aes-key-base64: ${LAB_MIND_MODEL_API_CONFIG_AES_KEY_BASE64}
    history-summary:
      enabled: true
      keep-recent-turns: 4
      compression-batch-turns: 6
      recent-transcript-max-chars: 2200
      summary-max-chars: 1400
    rewrite:
      correction-retry-enabled: true
    clarification:
      enabled: true
      max-options: 3
      min-top-score: 1.0
      ambiguous-score-gap: 0.8
    runtime:
      max-model-calls-per-run: 8
      max-model-calls-per-thread: 40
      max-tavily-tool-calls-per-run: 6
      max-tavily-tool-calls-per-thread: 30
      max-parallel-tools: 4
      tavily-max-retries: 2
      tavily-retry-initial-delay-ms: 200
      tavily-retry-max-delay-ms: 1200
    recommendation:
      enabled: true
      count: 3
      history-preview-turns: 4
      prompt: |
        你是推荐问题生成助手。
        请根据用户问题和助手回答，生成 3 个适合继续追问的中文问题。

        要求：
        1. 返回 JSON 对象中的 followUpSuggestionList 字段。
        2. 每个问题都要自然、具体、可继续追问。
        3. 不要和当前问题重复。
        4. 每个问题尽量控制在 28 个中文字符以内。
  kafka:
    topics:
      document-parse-requested: ${LAB_MIND_KAFKA_TOPIC_DOCUMENT_PARSE_REQUESTED:lab-mind.document.parse.requested}
      document-index-requested: ${LAB_MIND_KAFKA_TOPIC_DOCUMENT_INDEX_REQUESTED:lab-mind.document.index.requested}
      shadow-route-requested: ${LAB_MIND_KAFKA_TOPIC_SHADOW_ROUTE_REQUESTED:lab-mind.shadow.route.requested}
  knowledge:
    neo4j:
      uri: ${LAB_MIND_NEO4J_URI}
      username: ${LAB_MIND_NEO4J_USERNAME}
      password: ${LAB_MIND_NEO4J_PASSWORD}
      database: ${LAB_MIND_NEO4J_DATABASE}
    route:
      scope-top-k: 3
      topic-top-k: 5
      top-scope-topic-boost: 8
      top-scope-document-boost: 15
      top-topic-document-boost: 10
      lexical-weight: 1.0
      semantic-weight: 1.6
      success-confidence: 0.55
    retrieval:
      child-top-k: 40
      final-parent-top-k: 6
      max-parent-chars: 12000
      vector:
        top-k: 40
        min-similarity: 0.45
      keyword:
        top-k: 40
        relative-threshold: 0.35
      rrf:
        k: 60
      embedding:
        base-url: ${LAB_MIND_RETRIEVAL_EMBEDDING_BASE_URL}
        api-key: ${LAB_MIND_RETRIEVAL_EMBEDDING_API_KEY}
        model: ${LAB_MIND_RETRIEVAL_EMBEDDING_MODEL}
      pg-vector:
        url: ${LAB_MIND_PGVECTOR_URL}
        username: ${LAB_MIND_PGVECTOR_USERNAME}
        password: ${LAB_MIND_PGVECTOR_PASSWORD}
      elasticsearch:
        base-url: ${LAB_MIND_ELASTICSEARCH_BASE_URL}
        api-key: ${LAB_MIND_ELASTICSEARCH_API_KEY:}
        username: ${LAB_MIND_ELASTICSEARCH_USERNAME:}
        password: ${LAB_MIND_ELASTICSEARCH_PASSWORD:}
        index-name: ${LAB_MIND_ELASTICSEARCH_INDEX:lab_mind_document_chunk}
      rerank:
        enabled: ${LAB_MIND_RERANK_ENABLED:false}
        base-url: ${LAB_MIND_RERANK_BASE_URL:}
        api-key: ${LAB_MIND_RERANK_API_KEY:}
        model: ${LAB_MIND_RERANK_MODEL:}
    document-storage:
      endpoint: ${LAB_MIND_MINIO_ENDPOINT}
      access-key: ${LAB_MIND_MINIO_ACCESS_KEY}
      secret-key: ${LAB_MIND_MINIO_SECRET_KEY}
      bucket: ${LAB_MIND_MINIO_BUCKET}
